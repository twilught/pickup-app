const API_BASE='http://localhost:4000';
async function loadUsers(){const el=document.getElementById('users');try{const r=await fetch(API_BASE+'/users');const d=await r.json();el.innerHTML='';d.forEach(u=>{const li=document.createElement('li');li.textContent=`${u.id}: ${u.name}`;el.appendChild(li);});}catch(e){el.textContent='Error';}}
async function loadItems(){const el=document.getElementById('items');try{const r=await fetch(API_BASE+'/items');const d=await r.json();el.innerHTML='';d.forEach(i=>{const li=document.createElement('li');li.textContent=`${i.id}: ${i.name} - ฿${i.price}`;el.appendChild(li);});}catch(e){el.textContent='Error';}}
loadUsers();loadItems();